template: "class"
name: "Text"
index: 5
description: >
  Die Text-Klasse ermöglicht das freie Platzieren von Text-Objekten auf der
  Bühne.
constructors:
  - Text()
  - Text(text, x, y, width)
  - Text(text, x, y, width, style)
  - Text(text, x, y, fullWidth, style)
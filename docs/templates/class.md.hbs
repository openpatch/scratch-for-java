{{!
  The landing page of a class section. It is deliberately empty: the class
  itself is documented on its constructor page, which sorts first because it
  carries index 0.
}}
---
name: {{ name }}
{{#if index}}
index: {{ index }}
{{/if}}
{{#if hide}}
hide: true
{{/if}}
---

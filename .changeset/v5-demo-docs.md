---
type: patch
---

Document the quiz, glide and volume demos.

They were added with the `ask`, `glide` and volume work but had no page, so they
only existed in the repository. Each now has one, with a recording for the two
that are worth watching.

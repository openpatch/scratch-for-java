---
type: patch
---

Add a "Coin Collector" demo that is built only from built-in assets.

The project folder holds four short Java files and no assets at all - every
costume, backdrop and sound is addressed by name. It shows built-in costumes,
a built-in animation through `addAnimation("walk", "alienGreen_walk%d", 2)`,
built-in sounds, and a built-in backdrop working together in a small game.
